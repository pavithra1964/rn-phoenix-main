class ModuleItem {
  constructor(key, modulename, type, status) {
    this.id = key;
    this.modulename = modulename;
    this.type = type;
    this.status = status;
  }
}

export default ModuleItem;
